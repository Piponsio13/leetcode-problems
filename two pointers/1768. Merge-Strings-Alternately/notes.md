We merge the two strings by alternating characters from each one. Starting from the first position, we append one character from `word1` and then one from `word2` while both strings still have characters. Once one string ends, we append the remaining characters from the other string. This works in linear time because each character is visited once, and it uses only the output string as extra space.
